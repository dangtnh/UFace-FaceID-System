# ========================
# 1. HỆ ĐIỀU HÀNH (OS Files)
# ========================
.DS_Store
Thumbs.db
Desktop.ini

# ========================
# 2. PYTHON (Backend)
# ========================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# Môi trường ảo
venv/
.venv/
env/
# Công cụ test/coverage
.coverage
htmlcov/
.pytest_cache/

# ========================
# 3. NODE.JS & FRONTEND (QUAN TRỌNG - Bị thiếu ở file cũ)
# ========================
# Thư viện Node (Cực nặng, tuyệt đối không push)
node_modules/
frontend/node_modules/

# Logs lỗi của npm/yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Thư mục build của Vite/React (Code đã biên dịch, không cần push)
dist/
build/
frontend/dist/
frontend/build/

# ========================
# 4. DOCKER & DATABASE
# ========================
# Dữ liệu DB nếu mount ra ngoài (Tránh push cả cái Database lên Git!)
postgres_data/
pgdata/
.docker/

# ========================
# 5. MÔI TRƯỜNG & BẢO MẬT
# ========================
.env
.env.*
!.env.example
# File key SSH hoặc chứng chỉ SSL nếu có
*.pem
*.key

# ========================
# 6. DATA & AI MODELS
# ========================
# Các file weights nặng (Nên dùng Git LFS nếu muốn push, còn không thì ignore)
*.pt
*.pth
*.h5
*.onnx
*.tflite

# Vector database index
*.index
*.pkl

# Dữ liệu ảnh test/train (Giữ lại folder nhưng không push ảnh)
data/images/*
!data/images/.gitkeep

# Thư mục chứa weights
backend/weights/*
!backend/weights/.gitkeep

# ========================
# 7. IDE CONFIG (Tùy chọn)
# ========================
.vscode/
.idea/
*.swp